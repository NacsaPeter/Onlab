<UserControl
    x:Class="Lynn.Client.Views.TranslationExerciseView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Lynn.Client.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="308"
    d:DesignWidth="408">

    <Grid BorderThickness="2" BorderBrush="Black" CornerRadius="5" Background="LightCyan">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center"
                   Text="{Binding Sentence}" FontSize="28" />
        
        <TextBox Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                 Text="{Binding Translation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Margin="50,20,50,40" Padding="20" FontSize="22" KeyDown="EnterKeyDownEventHandler"
                 TextWrapping="Wrap" IsSpellCheckEnabled="False"/>
    </Grid>
</UserControl>
