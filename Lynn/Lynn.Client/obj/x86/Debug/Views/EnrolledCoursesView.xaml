<UserControl x:Class="Lynn.Client.Views.EnrolledCoursesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Lynn.Client.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="300">

    <Grid Background="White">
        <TextBox x:ConnectionId='1' x:Name="UserIDTextBox" Width="50" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10 10 0 0" PlaceholderText="UID" Text="{Binding UserID, Mode=TwoWay}"></TextBox>
        <Button x:ConnectionId='2' x:Name="ListEnrolledCoursesButton" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="85 10 0 0" Content="List enrolled courses" Command="{Binding ListEnrolledCourses_Click}"></Button>
        <ListView x:ConnectionId='3' x:Name="EnrolledCoursesListView" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10 50 0 0" Width="280" Height="240" ItemsSource="{Binding Courses}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding CourseName}"></TextBlock>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>

